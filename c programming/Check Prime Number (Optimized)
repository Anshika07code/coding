#include <stdio.h>
int main() {
    int n = 29, i, flag = 1;
    for (i = 2; i * i <= n; i++) {
        if (n % i == 0) {
            flag = 0;
            break;
        }
    }
    printf(flag ? "Prime" : "Not Prime");
    return 0;
}
