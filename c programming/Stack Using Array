#include <stdio.h>
#define N 5
int s[N], top = -1;
void push(int x) {
    if (top == N - 1) return;
    s[++top] = x;
}
void pop() {
    if (top == -1) return;
    top--;
}
int main() {
    push(10); push(20); pop();
    printf("%d", s[top]);
    return 0;
}
