#include <stdio.h>
#include <string.h>
int main() {
    char s[50] = "CProgramming";
    char *p = s;
    int i, n = strlen(s);
    for (i = n - 1; i >= 0; i--)
        printf("%c", *(p + i));
    return 0;
}
