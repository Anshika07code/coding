using System;

class Program
{
    static void Main()
    {
        int n = 121, temp = n, rev = 0;
        while (temp > 0)
        {
            rev = rev * 10 + temp % 10;
            temp /= 10;
        }
        Console.WriteLine(n == rev ? "Palindrome" : "Not Palindrome");
    }
}
