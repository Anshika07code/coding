using System;

class Program
{
    static void Main()
    {
        string s = "CSharp";
        char[] arr = s.ToCharArray();
        Array.Reverse(arr);
        Console.WriteLine(new string(arr));
    }
}
