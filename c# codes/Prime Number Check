using System;

class Program
{
    static void Main()
    {
        int n = 7, count = 0;
        for (int i = 1; i <= n; i++)
            if (n % i == 0) count++;
        Console.WriteLine(count == 2 ? "Prime" : "Not Prime");
    }
}
